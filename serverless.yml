# Serving HTML through API Gateway for AWS Lambda
service: night 

provider:
  name: aws
  runtime: nodejs4.3

functions:
  night:
    handler: handler.html
    events:
      - http:
          method: get
          path: / 
